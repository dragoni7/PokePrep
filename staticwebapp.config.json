{
  "routes": [
    {
      "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
          "/images/*.{png,jpg,gif}",
          "/css/*"
        ]
      }
    },
    {
      "route": "/pokedex",
      "redirect": "/pokedex"
    }
  ]
}